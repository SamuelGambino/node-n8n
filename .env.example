# Параметры обычного Node.js-хостинга.
PORT=3000
HOST=0.0.0.0

# Необязательный токен для защиты POST /v1/audit.
# При задании вызывающая система должна передавать заголовок:
# Authorization: Bearer <значение>
API_ACCESS_TOKEN=
